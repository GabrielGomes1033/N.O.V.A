# API criada pela NOVA

Este projeto foi gerado automaticamente pela assistente NOVA.

## Como executar

Instale as dependências:

```bash
pip install -r requirements.txt
```

## Flask

```bash
python app.py
```

## FastAPI

```bash
uvicorn app:app --reload
```
